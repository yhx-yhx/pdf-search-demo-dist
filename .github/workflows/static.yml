name: Vitepress Deploy

on:
  push:
    branches:
      - main # ����ӦΪ���Ĭ�Ϸ�֧��

jobs:
  deploy:
    runs-on: ubuntu-latest # ���Ը�����Ҫ���Ĳ���ϵͳ����
    steps:
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          publish_dir: ./
